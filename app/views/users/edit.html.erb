<% provide(:button_text, "変更する") %>
<div class="jumbotron jumbotron_width">
  <div class="form_container">
    <h1>プロフィールの変更フォーム</h1>
    <%= image_tag @user.avatar, size: '100x100', alt: "User Image" if @user.avatar.attached? %>
    <%= render 'form' %>
  </div>
</div>